import loading from '../assets/loading.webp'

const Loading = () => {
    return (
        <div className="loading flex_flex">
            <img src={loading} alt="loading..." />
        </div>
    )
}

export default Loading